# use ibs;
use collector;
# insert into collector.collector_currency select * from ibs.collector_currency;
# insert into collector.collector_company(companycode, companyname, parentcompany, currency_id) SELECT companycode,companyname,parentcompany,currency_id from ibs.collector_company ;
# insert into collector.collector_currencyrate select * from ibs.collector_currencyrate;
# insert into collector.collector_accounttype select * from ibs.collector_accounttype;
# insert into collector.collector_account(accountid, accountname, parentaccount, accounttypeid_id)  select * from ibs.collector_account;
